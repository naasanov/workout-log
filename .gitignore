# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
node_modules
/client/node_modules
/client/.pnp
/client/.pnp.js

# testing
/client/coverage

# production
/client/build
/dist

# test artifacts
/client/test-results/
/client/e2e/*.png
/client/e2e/*.jpg

# playwright
.playwright-mcp/

# misc
/.env
/request.rest
/client/.DS_Store
/client/.env
/client/.env.local
/client/.env.development.local
/client/.env.test.local
/client/.env.production.local

/client/npm-debug.log*
/client/yarn-debug.log*
/client/yarn-error.log*

*.orig
.idea/
.vscode/
# Agent worktrees live inside .claude/ but must never be committed — they are
# git worktrees of this same repo. (.claude/skills/**/SKILL.md IS tracked: it's
# shared repo knowledge. node_modules under .claude/ is covered by the rule above.)
.claude/worktrees/

# Scratch artifacts from agent/browser-verify runs and issue triage. These get
# dropped in the repo root by Playwright screenshots and the issue-orchestrator
# skill; none of them are repo content.
/*.png
/*.jpg
/ISSUE_PLAN.md
